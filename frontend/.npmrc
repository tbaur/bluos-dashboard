# Fail install if a dependency runs lifecycle scripts not listed in package.json allowScripts.
strict-allow-scripts=true
# Make the documented Node 22+ requirement fail the install rather than the build.
engine-strict=true
fund=false
